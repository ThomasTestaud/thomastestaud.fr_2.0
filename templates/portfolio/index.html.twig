{% extends 'base.html.twig' %}

{% block title %}Portfolio |
	{{ parent() }}
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
	<link rel="stylesheet" href="{{ asset('style/portfolio.css') }}">
{% endblock %}

{% block nav %}
	<a href="/">
		Accueil
	</a>
	<a href="/CV">
		Consulter le CV
	</a>
{% endblock %}

{% block body %}
	{% for article in articles %}
		<article class='article-project'>
			<div class="scroll-up">
				<i class=" fa-solid fa-arrow-up"></i>
			</div>

			<div class="project">
				<div class="project-el-1">
					<h2>{{article.title}}</h2>
				</div>
				<div class="project-el-2">
					<p>{{article.description}}</p>
				</div>
				<div class="project-el-3">
					<ul class="techs">
						{% for tech in article.techs %}
							<li>
								{{tech}}
							</li>
						{% endfor %}
					</ul>
				</div>
				<div class="project-el-4">
					<iframe src="{{article.link}}" frameborder="0"></iframe>
				</div>
			</div>
			<div class="scroll-down">
				<i class="fa-solid fa-arrow-down"></i>
			</div>
		</article>
	{% endfor %}
{% endblock %}
